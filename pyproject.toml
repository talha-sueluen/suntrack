[tool.black]
line-length = 88

[tool.flake8]
max-line-length = 88
exclude = [".git", "__pycache__"]

[project]
name = "suntrack"
version = "0.1.0"
requires-python = ">=3.10"

[project.dependencies]
dependencies = [
    "requests",
    "python-dotenv",
]